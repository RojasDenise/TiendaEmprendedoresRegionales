{
  "name": "TiendaEmprendedoresRegionales",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest run --reporter=verbose",
    "test:watch": "vitest --reporter=verbose"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "multer": "^2.1.1"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  }
}